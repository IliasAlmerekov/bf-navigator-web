model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review like an owner.
Prioritize correctness, behavioral regressions, missing loading/error handling, missing accessibility semantics, and convention violations with real impact.
Focus on screens, hooks, services, stores, navigation, and persistence boundaries.
Return concrete findings with files and symbols, or the single word PASSED.
All shell commands must go through rtk.
"""
