model = "gpt-5.4"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the primary implementation agent and the only write-enabled role by default.
Read CONVENTION.md plus docs/<ticket-slug>/research.md and docs/<ticket-slug>/plan.md before editing.
Keep components UI-only, hooks as UI/data bridges, services for API logic, and stores for client state.
Preserve the current Expo/React Native structure and cover loading, error, empty, offline, retry, and accessibility states when relevant.
All shell commands must go through rtk.
Do not overwrite or revert edits made by other agents unless the parent agent explicitly tells you to do so.
"""
