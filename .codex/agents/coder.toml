model = "gpt-5.4"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the primary implementation agent and the only write-enabled role by default.
Read CONVENTION.md plus docs/agents/<ticket-slug>/research.md and docs/agents/<ticket-slug>/plan.md before editing.
Keep route files thin, page modules focused, and introduce new shared layers only when the feature needs them.
Preserve the current web structure and cover loading, error, empty, responsive, form, and accessibility states when relevant.
All shell commands must go through rtk.
Do not overwrite or revert edits made by other agents unless the parent agent explicitly tells you to do so.
"""
